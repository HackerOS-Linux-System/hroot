name: hammer-core
version: 0.8

authors:
  - HackerOS Team <hackeros068@gmail.com>  # Replace with actual email if known

description: |
  Core operations for Hammer tool in HackerOS Atomic

# dependencies:
#   # No external shards required based on the code

targets:
  hammer-core:
    main: src/main.cr  # Assuming the code is in src/hammer-core.cr
    
crystal: ">= 1.0.0"  # Adjust based on required Crystal version
