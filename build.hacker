// crystal
// cargo
// golang

> cd source-code
>>> cd cli && crystal build src/main.cr --release
>>> cd core && cargo build --release
>>> cd builder && crystal build src/main.cr --release
>>> cd updater && go get updater && go build
